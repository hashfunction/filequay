# FolderSail cosmetic pointer settling

Screenshot-only run `35706490358`, source `81feaa956f274d9b2463633516596921edfaf7c7`, failed before its first PNG because the new tooltip scan dereferenced `ControlType.ProgrammaticName` on unrelated controls with null/empty roles. The original failure tree identifies `ContextCommandBar`/`ApplicationBar`, `BaseCommandBar`/`ApplicationBar` and `RootGridZoom`/`SemanticZoom`. Artifact **10684860564** is retained unchanged at `/private/tmp/foldersail-capture-35706490358-review/artifact/marketing-capture.json`; the original job log is in the same review directory. Owned process/package/profile/demo/certificate/key/temp cleanup and display restoration passed; normal close and screenshots were not reached.

The cosmetic pointer step now performs its one original ownership-checked move, verifies immediate readback, waits 500 ms, checks readback again, and revalidates the owned frame/content. It performs no UIA tooltip traversal, input retry or pixel manipulation. Metadata records the requested and measured settling interval plus pointer readback; it no longer asserts tooltip absence. Original PNGs require visual review. Application qualification, package binding, receipt/CSV checks, capture geometry and cleanup gates remain unchanged.

Focused verification: the updated production-pointer fixture first rejected the old tooltip-absence metadata, then passed with actual elapsed settling time and the original three null-role nodes. It verifies that no selector traversal occurs, foreign frames receive no move, incorrect readback refuses, and successful positioning sends exactly one move. Existing receipt-header positives/six refusals and script/helper integration also pass; whitespace checks pass. Native screenshot recapture remains required; no broader suite or app build was repeated.
