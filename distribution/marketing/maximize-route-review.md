# FolderSail observed native Maximize action

Screenshot-only run `35707464858`, source `654f361d616bbb3f83bd8c69d7981d9564d45a22`, failed before pointer placement or any PNG: `Timed out waiting for actual maximized FolderSail window: No matching observation.` Its `WindowPattern.SetWindowVisualState(Maximized)` call returned without achieving the native maximized state within the original 30-second observation budget. This does not establish a product defect.

Artifact **10685870274**, `FolderSail-real-product-screenshots`, is retained unchanged at `/private/tmp/foldersail-capture-35707464858-review/artifact/marketing-capture.json`; the original job log is `/private/tmp/foldersail-capture-35707464858-review/capture-job.log`. The failure tree shows main HWND 524622/PID 5344, `Inbox - FolderSail`, and one visible enabled `Maximize`/`Maximize` control with role `ControlType.Button` beneath the native caption controls. A second same-ID/name button is explicitly offscreen. The original discovery helper excludes that hidden duplicate. Actual desktop mode was 1920×1080. Owned process/package/profile/demo/certificate/key/temp cleanup and display restoration passed; normal UI close was not reached.

Capture now discovers that exact visible Maximize button within the owned main window and invokes it once through the unchanged qualified input helper. That helper retains the original foreground, PID/handle, ownership and enabled/visible checks. The caption role/PID/window must also match. An already-maximized window receives no action. The existing native maximized-state observation and subsequent full frame/content/geometry checks remain. There is no action retry or alternate-input fallback. Before/last-after native frame facts and action choice are retained for any further failure. No application binary, qualified helper, package binding, real file/receipt/CSV workflow or cleanup gate changes.

Focused verification: seven production caption-route cases pass (normal, already maximized, foreign HWND, foreign PID, wrong role, action failure and no native state change). The fixture asserts one action maximum, no foreign action, the original action failure and refusal when the native state never changes. It uses native/UI seams locally and does not claim Windows execution. Existing script/helper integration and whitespace checks pass. A fresh actual capture remains necessary; no broad suite or app build was repeated.
