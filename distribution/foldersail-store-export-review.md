# FolderSail assigned identity, native notices and unsigned export

This candidate adds a separate assigned-identity Consumer qualification and a final unsigned Store export. It does not claim that a Store package has passed Windows qualification or has been submitted. Both original Consumer and Instrumented jobs remain independent and mandatory. Store export runs on a third fresh Windows runner only after both jobs succeed with `RequireClean`.

The assigned identity is `1659hashfunction.FileQuay`, publisher `CN=B6A2631A-FD32-45CC-AE12-82466975F528`, publisher display `hashfunction`, family `1659hashfunction.FileQuay_r3hxytd7jt6c4`, version `1.0.1.0`, and executable `FolderSail.exe`. The existing application ID remains **App**. The legacy protocol/settings identities are preserved. Store mode rejects Instrumented and AllowPreinstalled configurations; default disposable identity behavior and output paths are retained.

The Store runner downloads both metadata artifacts from the same workflow run, then executes the original complete native qualification with Store identity in dedicated `Store-Consumer` directories. Manifest configuration is restored in `finally`, so the final exporter can require a clean exact Git tree. All original provider/native adapter, input/ownership, copy/move, Unicode file bytes, receipt/CSV, cancellation/recovery, normal activation/close, process outcome, package/trust cleanup and reporting gates remain intact. The only consumer observation addition retains already verified fictional CSV bytes and cleared history in the receipt before their existing cleanup. Seven actual installer failure/ownership scenarios execute against the selected identity mode.

`export_store.py` requires all three current source/run/attempt build and installed receipts. It rechecks the semantic unpack inventory, unchanged unsigned ZIP bytes, exact identity, actual loaded module paths/hashes, compiled Consumer/Instrumented kinds, original consumer byte/CSV/visible-receipt evidence, and all original cleanup results. It repeats the existing CSV header, row-width and timestamp comparisons, preserving .NET's seventh fractional timestamp digit. It preserves the original accepted Consumer background-process close outcome and the existing disposable-runner framework retention policy; it does not silently replace those policies with new acceptance rules.

Runtime verification binds the original native 7-Zip, libgit2 and Win2D hashes; exact current .deps/runtime notices; the audited stable Windows SDK .NET projection 10.0.26100.70 original archive and runtime metadata; both .NET 10.0.12 entrypoint runtime-pack byte checks; exact freshly registered Microsoft Windows App Runtime 2.4.0.0 artifact; and SQLite package/native source ID, bytes and actual execution checks. Managed ReadyToRun hashes bind to each fresh build rather than being assumed equal to original NuGet DLL hashes.

The distributable now retains original notice bytes and source/build records through `FileQuay.CorrespondingSource.props`. The notice map covers 87 actual selected runtime packages plus native 7-Zip. The retained original Windows `.deps.json` from 34694585269 is an executable graph fixture: its former preview SDK key is rejected; only the independently approved stable SDK substitution matches the reviewed package set. That baseline run is inventory evidence, not a successful Consumer acceptance claim. Existing application and SQLite notices remain intact.

Six corresponding-source assets and the exact manifest are publicly available at https://github.com/hashfunction/filequay/releases/tag/native-sources-2026-09-12. Root independently published and anonymously verified all seven files. The production downloader independently repeated that check locally: **22,151,074 bytes**, all seven exact sizes and SHA-256 values matched. Its small receipt is retained at `/private/tmp/foldersail-source-publication-verifier.json`. The two approved media-free source archives were reproduced again from the unchanged upstream originals; both archive hashes and the full deterministic derivation receipt matched. The package includes build/replacement instructions at `Licenses/CorrespondingSource/README.md`, and canonical source location https://foldersail.trieflow.com/source.

Each export anonymously downloads the seven public assets again and verifies the current application source archive against every tracked Git blob/mode of the exact build commit. It then rechecks current source, all evidence and original package bytes before/after copying. Only `FolderSail_1.0.1.0_x64.msix` and its separate `release-ready.json` are uploaded by the successful Store job. Signed test packages, certificate/private-key material and Instrumented binaries are not in that artifact allowlist. Historical qualification flags remain historical; the final receipt is separate and `submitted` remains false.

## Exact provenance limitations

Win2D 1.3.2's original NuGet `Win2d.githash.txt`, native ProductVersion and managed informational version identify `feefabe2d8ef80025030c1dd7c9d3d71e2462cd3`, unavailable from the public Microsoft repository endpoint. Its native DLL exactly matches the observed payload. The included Microsoft MIT source notice is attributed to its own separately observed immutable revision, **not** claimed as the source/license file that produced this package. The NuGet's original Microsoft Win2D EULA URL now redirects, but its original Microsoft content has since been recovered from two byte-identical raw archived captures and retained in full. The source gap remains distinct from that recovered binary redistribution agreement. Exact scoped Store license text (including every English/French provision) is packaged and must be delivered through the Store license field before submission; see `foldersail-win2d-terms-review.md`. These distinctions remain explicit in the runtime map, user source guide, and final receipt. Native sources were not rebuilt reproducibly; no actual Windows modified-library replacement demonstration is claimed. No generic URL or unrelated commit is presented as exact Win2D corresponding source.

## Local verification

- `PATH="$PWD/.tools/dotnet-10.0.401:$PATH" DOTNET_ROOT="$PWD/.tools/dotnet-10.0.401" TMPDIR=/private/tmp python3 -m unittest discover -s tests/packaging -v`: **62 tests passed**. Includes real MSBuild notice links, real Windows-style Git checkout preserving original notices, actual retained runtime graph, source-publication/tar tamper, exact Git commit/tree/dirty refusal, three lifecycle refusal, unsigned ZIP/identity/notice bytes, output copy, and mutation during publication.
- `.tools/powershell-7.6.6/pwsh -NoProfile -File tests/packaging/test-store-identity.ps1`: **22 checks passed**.
- `.tools/powershell-7.6.6/pwsh -NoProfile -File tests/packaging/test-store-orchestration.ps1`: actual coordinator order and both failure short circuits passed.
- `test-installation-failures.ps1`, all seven scenarios with both `-IdentityMode Qualification` and `-IdentityMode Store`: **14 actual installer failure replays passed**; no native install is claimed by these fixtures.
- Actual anonymous downloader verified the published seven files, and exact upstream source-only derivation was rerun successfully.

A fresh Windows build plus all three installed lifecycles and final public-current-source verification remain required before a Store-ready artifact can exist. Root owns snapshot/push, native dispatch, any Store submission and website publication. The next existing native run failure is a separate diagnostic/repair task, not folded into this source/export candidate.
